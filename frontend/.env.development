# 本地环境
ENV='development'

# 前端端口（80 端口需要管理员权限，无端口号访问）
VITE_PORT=80

# 本地环境接口地址（后端运行在8100端口）
VITE_API_BASE_URL='http://127.0.0.1:8100'

# 后端接口前缀
VITE_API_PREFIX='/api'

# 静默Sass弃用警告
SASS_SILENCE_DEPRECATIONS='legacy-js-api,import'